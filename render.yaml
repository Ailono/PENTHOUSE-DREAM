services:
  - type: web
    name: penthouse-dream-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && uvicorn app:app --host 0.0.0.0 --port ${PORT:-10000}
    envVars:
      - key: BOT_TOKEN
        value: 8976221169:AAG5zZ6tisXvLTn4N1WKujAHQmQfVkwX9P8
      - key: RENDER_URL
        sync: false
      - key: DB_TYPE
        value: sqlite
